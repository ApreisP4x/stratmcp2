# Core dependencies
fastmcp>=2.0.0
pydantic>=2.0.0

# HTTP server (included with fastmcp but explicit for clarity)
uvicorn>=0.20.0
starlette>=0.27.0

# Development dependencies
pytest>=7.0.0
pytest-asyncio>=0.21.0
